# Radiative Heating Prompt-to-Artifact Checklist

Date: 2026-05-14

This checklist maps the user-requested objective to concrete local evidence.
It is intentionally stricter than the executable smoke audit: proxy artifacts
are recorded as evidence, but they do not close requirements that ask for
ecRad/ecCKD references, H100/Nsight profiles, production Breeze behavior, or
full differentiable optimization.

| Requirement | Evidence | Status |
|---|---|---|
| Review `radiative_heating.md`, identify metrics, and edit unclear metrics | `radiative_heating.md` now defines accuracy thresholds, runtime/memory metrics, GPU/Nsight gates, RCEMIP-style benchmark metadata, reduced-model Pareto reporting, and completion blockers | Partial: metrics are defined and H100 evidence exists, but final ecRad/reduced-model accuracy still fails |
| Combine discussed objectives into one concrete `/goal` objective | `radiative_heating.md` section 0 and `validation/goal_audit.md` state the objective as a standalone RadiativeHeating package plus dedicated Breeze extension with ecRad/ecCKD accuracy, H100 speedup, Reactant/Enzyme optimization, and gas-optics training | Done as a written objective; goal is not achieved |
| Add the three required gates to the `/goal` definition | `radiative_heating.md` section 0 and `validation/goal_audit.md` now define the three gates: cloudless official-ecCKD ecRad accuracy, reduced 32/32b/16-term gas-optics optimization/training evidence, and current IFS all-sky plus Breeze H100 speedup evidence | Done as written gates; only the first focused cloudless gate and the H100 speed gate currently pass |
| Delete the old ABR-owned Breeze extension | `ext/AnalyticBandRadiationBreezeExt.jl` is deleted; `validation/results/goal_audit_check.json` verifies the old ABR-side Breeze extension is absent | Done |
| Clone a fresh dedicated Breeze checkout and do not touch other Breeze copies | Dedicated checkout is `/shared/home/greg/Projects/BreezeRadiativeHeatingDev/Breeze.jl`; audit records origin `https://github.com/NumericalEarth/Breeze.jl` and HEAD `8a3dba0575a7b8c29cb8dfedc5fe391cab7d2938` | Done for local development |
| Implement `BreezeRadiativeHeatingExt` in Breeze | `/shared/home/greg/Projects/BreezeRadiativeHeatingDev/Breeze.jl/ext/BreezeRadiativeHeatingExt/BreezeRadiativeHeatingExt.jl` implements `RadiativeHeatingOptics()` construction, fixed/tabulated ecCKD workspaces, separate column flux/heating access points, CPU support, and the H100 device path used by the RCEMIP benchmark | Partial: clear-sky production benchmark path exists; cloud/aerosol coupling and reduced accuracy remain open |
| Make the design light to mesh with Breeze and SpeedyWeather-style host models | ABR exports component access points for gas optics, grid-mean cloud optics, cloudy-region cloud optics, aerosol optics, solvers, fluxes, and heating rates; `validation/access_points_check.jl` passes; Breeze extension exposes separate `fill_column_state!`, `radiative_heating_column_fluxes!`, and `column_heating_rates!` entry points | Partial: access points and CPU component path exist; production GPU/device path remains open |
| Allow host models to own solvers or vertical integrals | `validation/access_points_check.jl` verifies stopping after gas optics and replacing solver/vertical-integral paths | Done as API scaffold; more host integrations still need production validation |
| Implement ecRad-style solver separation | `src/runtime_interfaces.jl`, `src/solvers/cloudless_longwave.jl`, `src/solvers/cloud_overlap_longwave.jl`, `src/solvers/cloudless_shortwave.jl`, and `src/solvers/cloud_optics.jl`; tests cover clear-sky solvers plus absorptive cloud and aerosol optics; `CloudlessLongwave` now has an opt-in ecRad-style longwave scattering adding path with a zero-SSA regression test; `CloudOverlapLongwave` has clear/full-cloud/finite mixed-cloud tests plus a Tripleclouds-style mode using separate source and albedo overlap matrices | Partial: full McICA/Tripleclouds source coupling, remaining longwave/shortwave transport details, exact cloud overlap, aerosol scattering/asymmetry, and ecRad all-sky comparison remain open |
| Ingest/evaluate ecCKD gas optics | `src/io/ecckd_definition.jl`, `ext/AnalyticBandRadiationNCDatasetsExt.jl`, and `src/gas_optics/ecckd_forward.jl`; unit tests pass; `validation/ecckd_official_files_check.jl` verifies the official ecRad checkout ecCKD LW 64-gpoint and SW 32-gpoint definition files are present, schema-recognized, and can be sampled into the runtime tabulated LUT path with finite nonnegative optical depths; Breeze extension exercises fixed and tabulated ecCKD runtime models; ecRad NetCDFs are materialized and schema-valid | Partial: official ecCKD files now feed the lightweight runtime LUT bridge, but the bridge samples the H2O mole-fraction axis and compresses the official pressure-dependent temperature grid, so hard ecRad/ecCKD numerical thresholds still fail |
| Add optional RRTMGP extension for direct package-native comparisons | `ext/AnalyticBandRadiationRRTMGPExt.jl` computes RRTMGP fluxes from `ColumnAtmosphere` into `RadiativeFluxes`; `test/test_rrtmgp_ext.jl` verifies direct package-native comparison plumbing | Done for clear-sky direct comparison |
| Define hard acceptance accuracy thresholds | `validation/ecrad_reference_manifest.jl` and `validation/results/ecrad_reference_manifest.json` define required files, variables, and hard threshold fields; `validation/materialize_ecrad_references.jl` materializes upstream ecRad references into that schema; `validation/write_ecrad_candidates.jl` writes first-pass `radiative_heating_*` and `radiative_heating_*_clear` candidate variables; `validation/ecrad_candidate_schema.jl` checks that candidate variables exist and match reference shapes; `validation/ecrad_accuracy_gate.jl` applies flux, heating-rate, TOA net-flux, and surface net-flux thresholds; `validation/ecrad_cloudless_accuracy_gate.jl` verifies the first clean ecCKD cloudless/no-aerosol gate; `validation/ecrad_accuracy_diagnostics.jl`, `validation/ecrad_all_sky_cloud_effect_diagnostics.jl`, `validation/ecrad_all_sky_cloud_sweep.jl`, and `validation/ecrad_all_sky_optics_gap.jl` rank current full-gate, cloud-effect, and optical-property failures; `validation/ecrad_convention_gap_report.md` records the reference/candidate convention gap; synthetic NetCDF tests prove schema pass/fail and threshold pass/fail behavior | Partial: the clean ecCKD cloudless gate passes, but the full all-sky hard ecRad gate still fails; cloudy-region cloud optics are now represented but the Tripleclouds/McICA solver and ecRad cloud optical tables are not implemented |
| Ingest official ecRad cloud scattering tables for the all-sky gate | `src/io/cloud_scattering.jl`, `test/test_cloud_scattering_table.jl`, and `validation/ecrad_cloud_scattering_tables_check.jl` read and verify the official liquid-water and ice scattering tables from the ecRad checkout, map them to official SW 32-gpoint and LW 64-gpoint ecCKD grids, expose `add_mapped_cloud_scattering!`, apply ecRad-style shortwave cloud delta-Eddington scaling before transport, and `validation/ecrad_all_sky_cloud_sweep.jl` now targets the matched `ecckd_all_sky_tropical_column` reference and identifies `table_scattering_tripleclouds_alpha_cf1_cloudy_region` as the best current matched ecCKD all-sky trial; 2x/3x table-extinction scaling, IFS aerosol-table variants, shortwave cloud-SSA scaling diagnostics, split SW/LW inhomogeneity exponents, opt-in LW cloud-scattering, and longwave-overlap trials worsen the gate | Partial: table ingestion, g-point mapping, runtime shortwave composition, opt-in LW scattering, opt-in LW overlap, matched reference-optics shortwave Tripleclouds transport evidence, and matched ecCKD all-sky diagnostic evidence are verified; hard matched ecCKD all-sky errors remain about 36.01/52.25 W m^-2 at TOA/surface |
| Add first explicit clear/cloudy all-sky overlap access point | `src/solvers/cloud_overlap_shortwave.jl`, `src/solvers/cloud_overlap_longwave.jl`, `test/test_cloudless_shortwave_solver.jl`, `test/test_cloudless_longwave_solver.jl`, and `validation/write_ecrad_candidates.jl` add `ShortwaveCloudOverlapOpticalProperties`, `CloudOverlapShortwave`, `LongwaveCloudOverlapOpticalProperties`, `CloudOverlapLongwave`, and opt-in shortwave/longwave overlap candidate paths; `validation/ecrad_reference_optics_solver_gap.jl` confirms shortwave `tripleclouds_alpha_p2` agrees with the matched ecCKD Tripleclouds reference to about 1e-5 W m^-2 when fed ecRad optical properties | Partial: shortwave Tripleclouds transport is verified with reference optics, but hard end-to-end all-sky thresholds still fail because cloud/aerosol/longwave optical-property composition remains open |
| Compare against RRTMGP on a realistic RCEMIP-style problem | Fresh Breeze checkout has `benchmarking/radiative_heating_rcemip_benchmark.jl`; `benchmarking/results/rcemip_h100_32x32x64/radiative_heating_rcemip_latest.json` records a 32 x 32 x 64 H100 workload with 1024 columns and both RadiativeHeating and RRTMGP runtimes | Done for the performance comparison workload |
| Demonstrate at least 4x speedup over RRTMGP in production Breeze context | The 32 x 32 x 64 H100 artifact records `45.299706770740976x` speedup, `final_4x_claim_supported=true`, and both Nsight report paths | Done for the requested H100 performance gate |
| Use profiling extensively, especially Nsight on H100 | Breeze checkout includes `benchmarking/radiative_heating_gpu_environment_check.jl`, `benchmarking/radiative_heating_h100_acceptance.sh`, `/shared/home/greg/nsight_h100_runbook.md`, and Nsight Systems/Compute reports under `benchmarking/results/rcemip_h100_32x32x64/nsight/` | Done for the current H100 benchmark |
| Focus H100 for headline performance | H100 preflight artifact reports ready and the final 32 x 32 x 64 H100 benchmark supports the 4x claim | Done for the headline performance case |
| Produce reduced 32/32b and 16-term runtime/memory/accuracy reports | Fresh Breeze checkout has H100 reduced runtime artifacts for 16/16 and 32/16 and aggregate reduced reports under `benchmarking/results/reduced_pareto/` and `benchmarking/results/reduced_accuracy/`; `validation/reduced_ecckd_accuracy.jl` records that the official 32x32 clean ecCKD model passes, while the best current weighted 32x16 shortwave subset reduces worst TOA/surface forcing misses to about 5.38/3.05 W m^-2 but still fails the 0.3 W m^-2 hard gate; `validation/reduced_ecckd_size_scan.jl` shows simple official-gpoint reductions only pass at the full 32 SW g-points | Partial: reduced H100 runtime and improved 32x16 accuracy evidence exist; hard reduced 16-term accuracy remains blocked |
| Implement ecCKD optimization using Reactant and Enzyme | `validation/toy_ecckd_training.jl` records finite-difference training, Enzyme pure two-stream gradient check, and Reactant toy compilation | Partial: toy fixed-topology proof only; full production mutating radiative-transfer AD remains open |
| Demonstrate end-to-end training of gas-optics model | `validation/results/toy_ecckd_training.json` records loss reduction and improved toy flux/heating RMSE | Partial: toy training only; CKDMIP-scale or production ecCKD training remains open |
| Provide completion audit | `validation/goal_audit.md`, `validation/goal_audit_check.jl`, and `validation/results/goal_audit_check.json/.md` record current evidence and blockers | Done; audit status is correctly `not_complete` |

Current non-negotiable blockers before `/goal` can be marked complete:

1. Pass the full `validation/ecrad_accuracy_gate.jl` hard ecRad threshold
   suite. The first clean ecCKD cloudless/no-aerosol gate now passes, but the
   full suite still fails on all-sky and legacy non-ecCKD cases.
2. Implement reduced ecCKD/RRTMG-compatible 32/32b/16-term models with hard
   reference-threshold pass/fail reports. The official 32/32b clean ecCKD path
   passes the first gate, but reduced 32/16 evidence still fails thresholds.
3. Implement current IFS all-sky conventions: liquid and ice cloudy-region
   optical properties, aerosol optics, scattering, cloud fraction/overlap,
   inhomogeneity, and Tripleclouds/McICA-style solver semantics.
4. Extend the differentiable workflow beyond toy fixed-topology checks to
   production gas-optics optimization/training.
